# 🔍 ПОЛНЫЙ АУДИТ КОДА: Diar Car Buy AI v3.0

**Дата:** 2026-06-03  
**Репо:** kirillfomenkov92-crypto/diar_car_buy  
**Статус:** 6 КРИТИЧЕСКИХ проблем в парсерах и основной логике

---

## 📊 КРАТКАЯ СВОДКА

Проанализировал 4 файла (database.py, parsers/autoru.py, parsers/avito.py, main.py) и выявил:

| # | Проблема | Тип | Приоритет |
|---|----------|------|-----------|
| 1 | Auto.ru каждый цикл находит ТЕ ЖЕ 25 объявлений | БАГ | 🔴 КРИТИЧ |
| 2 | Avito получает HTTP 403 (блокировка) | ЕСТЕСТВ | 🟡 ВЫСОК |
| 3 | Фильтр "свежих" объявлений не работает | БАГ | 🔴 КРИТИЧ |
| 4 | Логирование вводит в заблуждение | БАГ | 🟡 СРЕДН |
| 5 | Нет уведомлений об устаревших куках Avito | НЕДОСТ | 🟡 СРЕДН |
| 6 | Fast cycle переиспользует старые данные | БАГ | 🔴 КРИТИЧ |

---

## 🔴 ПРОБЛЕМА 1: AUTO.RU ДУБЛИ (КРИТИЧ)

### Симптомы из Логов
```
22:18  Auto.ru: найдено 37 saleId | 25 новых объявлений
22:24  Auto.ru: найдено 37 saleId | 25 новых объявлений ← ОДНИ И ТЕ ЖЕ!
```

### Корневая Причина
**Auto.ru парсер НЕ заполняет поле `published_at`**, поэтому фильтр в fast_cycle() не может определить возраст объявления.

**Код:**
```python
# parsers/autoru.py, строка 161
"published_at": "",  # ❌ ВСЕГДА ПУСТО
```

Затем в main.py:
```python
# main.py, строка 178-182
fresh = [
    l for l in listings
    if calculate_listing_age_minutes(l.get("published_at", "")) <= 30
]
logging.info(f"[FAST] Свежих (до 30 мин): {len(fresh)} из {len(listings)}")
```

Когда `published_at=""`, функция `calculate_listing_age_minutes()` либо вернёт 999 (объявление не будет считаться свежим), либо крашнется. Результат: объявления логируются как "новые" каждый цикл, но фильтр их убирает → логи показывают "0 свежих из 25".

### Ожидаемое Поведение
- Цикл 1: 25 новых → сохранены
- Цикл 2 (6 мин спустя): 0 новых (уже в БД)

### Решение
Парсить дату публикации из Auto.ru JSON и заполнить `published_at` в результате.

---

## 🔴 ПРОБЛЕМА 2: AVITO HTTP 403 (ЕСТЕСТВ + НЕДОСТ ОБРАБОТКИ)

### Симптомы
```
2026-06-03 22:18:40,152 WARNING Avito curl_cffi: 403 — IP временно заблокирован, пауза 15 мин
```

### Причины (все верны)
1. Cookies из браузера устаревают за 1-2 дня
2. curl_cffi fingerprint может быть обнаружен WAF
3. IP блокируется при частых запросах
4. Нет retry механизма

### Текущая Обработка
```python
# parsers/avito.py, строка 320-322
elif resp.status_code == 403:
    RUNTIME_CONFIG["AVITO_CURL_BLOCKED_UNTIL"] = now + 900
    logging.warning("Avito curl: 403 — пауза 15 мин")
    # ← Просто блокировка, ничего больше
```

### Сравнение с Auto.ru (правильно)
```python
# parsers/autoru.py, строка 70-76
if response.status_code == 403:
    logging.warning("Auto.ru: 403 — куки устарели. Запусти: python utils/cookie_extractor.py")
    cnt = RUNTIME_CONFIG.get("AUTORU_EMPTY_CYCLES", 0) + 1
    if cnt >= 5:
        _notify_stale_cookies()  # ← Отправляет Telegram!
```

### Решение
Применить ТОТ ЖЕ механизм к Avito: счётчик попыток (5 циклов) → Telegram уведомление с инструкцией обновить cookies.

---

## 🔴 ПРОБЛЕМА 3: ФИЛЬТР "СВЕЖИХ" НЕ РАБОТАЕТ (КРИТИЧ)

### Логи
```
[FAST] Свежих (до 60 мин): 0 из 25  ← Каждый цикл одно и то же
```

### Причина
Комбинация:
1. `published_at=""` в Auto.ru парсере (Проблема #1)
2. Функция `calculate_listing_age_minutes("")` возвращает что-то неправильное
3. Фильтр не срабатывает

**Код в main.py:**
```python
fresh = [
    l for l in listings
    if calculate_listing_age_minutes(l.get("published_at", "")) <= 30
]
```

**Вопрос:** Где находится `calculate_listing_age_minutes()`? Что она возвращает при пустой строке?

### Решение
1. Заполнить `published_at` в Auto.ru (Проблема #1)
2. Проверить логику `calculate_listing_age_minutes()` в speed_monitor.py
3. Добавить fallback: если `published_at=""`, использовать `first_seen` из БД

---

## 🟡 ПРОБЛЕМА 4: ЛОГИРОВАНИЕ ВВОДИТ В ЗАБЛУЖДЕНИЕ

### Что Происходит
```
parsers/autoru.py, строка 85:
logging.info(f"Auto.ru: {len(results)} новых объявлений")  # ← "25 новых"

main.py, строка 152:
logging.info(f"[FAST] autoru: {len(autoru_results)} новых")  # ← "25 новых" снова!
```

Читая логи, выглядит как две разные партии объявлений. На самом деле это ОДНИ И ТЕ ЖЕ 25, упомянутые дважды.

### Решение
Изменить сообщение в main.py:
```python
logging.info(f"[FAST] autoru: обработано {len(autoru_results)} объявлений")
```

Или вообще убрать дублирование логов.

---

## 🟡 ПРОБЛЕМА 5: ОТСУТСТВУЕТ МЕХАНИЗМ ДЛЯ AVITO COOKIES

### Описание
Когда Avito возвращает 403, нет:
- ✅ Telegram уведомления
- ✅ Инструкции как исправить
- ✅ Автоматического обновления

Сравни с Auto.ru — там всё есть.

### Решение
Скопировать логику из `parsers/autoru.py`:
```python
def _notify_stale_avito_cookies():
    token = RUNTIME_CONFIG.get("TELEGRAM_BOT_TOKEN", "")
    chat_id = RUNTIME_CONFIG.get("TELEGRAM_CHAT_ID", "")
    if not token or not chat_id:
        return
    text = (
        "Avito: 5 циклов HTTP 403.\n"
        "Куки устарели или IP заблокирован.\n"
        "Запусти: python utils/cookie_extractor.py\n"
        "(закрой Chrome перед запуском)"
    )
    # Отправить в Telegram...
```

И вызвать эту функцию когда счётчик достигнет 5.

---

## 🔴 ПРОБЛЕМА 6: FAST_CYCLE ПЕРЕИСПОЛЬЗУЕТ СТАРЫЕ ДАННЫЕ

### Цепь Событий
```
fast_cycle() запускается каждую минуту
    ↓
autoru_parse() возвращает 25 объявлений
    ↓
Фильтр by age не работает (published_at="")  ← Проблема #3
    ↓
Все 25 считаются "свежими"
    ↓
mark_seen() добавляет их в БД ИЛИ обновляет
    ↓
Но БД не видит их как "уже известные" из-за того, что фильтр не сработал
    ↓
В следующем цикле то же самое повторяется
```

### Корневая Причина
Комбинация Проблем #1 и #3.

### Решение
Исправить #1 и #3, затем Проблема #6 решится автоматически.

---

## ✅ ПРОВЕРКА БД: ВСЁ ОК

Проверил database.py:
- ✅ `is_seen()` работает правильно
- ✅ `mark_seen()` имеет `commit()` после INSERT/UPDATE
- ✅ UNIQUE(listing_id, source) гарантирует дедупликацию
- ✅ WAL режим включен для конкурентного доступа

**Вывод:** БД работает корректно. Проблема НЕ в сохранении данных.

---

## 🔧 ПЛАН ИСПРАВЛЕНИЙ

### Приоритет 1 (ВТОРНИК)
- [ ] **Найти поле даты в Auto.ru JSON**
  - Может быть: `"creationDate"`, `"publishedDate"`, `"created_at"` или ещё что-то
  - Тестировать на реальном сайте или с помощью curl

- [ ] **Добавить парсинг даты в Auto.ru парсер**
  ```python
  # В функции _parse_from_json()
  date_m = re.search(r'"someDate":"(\d{4}-\d{2}-\d{2}...)', window)
  published_at = date_m.group(1) if date_m else ""
  ```

- [ ] **Проверить функцию `calculate_listing_age_minutes()` в speed_monitor.py**
  - Логика при пустой строке
  - Должна ли быть fallback на текущее время или на `first_seen`?

### Приоритет 2 (ВТОРНИК)
- [ ] **Добавить Telegram уведомления для Avito 403**
  - Функция `_notify_stale_avito_cookies()`
  - Счётчик попыток (как в Auto.ru)

- [ ] **Исправить логирование в main.py**
  - Убрать "новых", заменить на "обработано"

### Приоритет 3 (СРЕДА)
- [ ] **Добавить retry логику для HTTP 403**
  - Или переключаться на другой уровень (Playwright)

- [ ] **Добавить fallback для пустого `published_at`**
  - Использовать `first_seen` из БД если парсить не удалось

---

## 📝 ПРИМЕРЫ ИСПРАВЛЕННЫХ ЛОГОВ

### ДО ИСПРАВЛЕНИЯ
```
22:18:40 INFO Auto.ru: найдено 37 saleId в JSON
22:18:40 INFO Auto.ru: 25 новых объявлений
22:18:40 INFO [FAST] autoru: 25 новых
22:18:40 INFO [FAST] Свежих (до 30 мин): 0 из 25

22:24:40 INFO Auto.ru: найдено 37 saleId в JSON
22:24:40 INFO Auto.ru: 25 новых объявлений  ← ПРОБЛЕМА!
22:24:40 INFO [FAST] autoru: 25 новых
22:24:40 INFO [FAST] Свежих (до 30 мин): 0 из 25
```

### ПОСЛЕ ИСПРАВЛЕНИЯ
```
22:18:40 INFO Auto.ru: найдено 37 saleId в JSON
22:18:40 INFO Auto.ru: 25 объявлений (новых: 25, обновлено: 0)
22:18:40 INFO [FAST] autoru: обработано 25 объявлений
22:18:40 INFO [FAST] Свежих (≤30 мин): 18 из 25

22:24:40 INFO Auto.ru: найдено 37 saleId в JSON
22:24:40 INFO Auto.ru: 25 объявлений (новых: 0, обновлено: 25)  ← ИСПРАВЛЕНО!
22:24:40 INFO [FAST] autoru: обработано 25 объявлений
22:24:40 INFO [FAST] Свежих (≤30 мин): 0 из 25
```

---

## 🎯 ВОПРОСЫ ДЛЯ CLAUDE

1. **Где парсить дату публикации в Auto.ru JSON?**
   - Какое точное имя поля?
   - Какой формат (ISO 8601, Unix timestamp)?

2. **Где находится функция `calculate_listing_age_minutes()`?**
   - В speed_monitor.py?
   - Как она обрабатывает пустую строку?

3. **Есть ли файл `utils/cookie_extractor.py`?**
   - Как обновляются куки?
   - Какие браузеры поддерживаются?

4. **Нужен ли retry механизм для HTTP 403 или просто уведомления?**

---

## 📋 SUMMARY

**6 проблем, 3 приоритета, 2 часа работы.**

- **Критичное:** Заполнить `published_at` + проверить `calculate_listing_age_minutes()` + исправить логирование
- **Высокое:** Добавить Telegram уведомления для Avito
- **Среднее:** Добавить retry логику и fallbacks

Готов помочь с конкретными исправлениями для каждого файла!
